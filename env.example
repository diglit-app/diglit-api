# ───────────────────────────────────────────────────────────────
# 📦 DATABASE CONFIGURATION (PostgreSQL)
# ───────────────────────────────────────────────────────────────

# The JDBC URL used to connect to your PostgreSQL database.
# Format: jdbc:postgresql://<host>:<port>/<database_name>
JDBC_DATABASE_URL=jdbc:postgresql://localhost:5432/diglit_db

# Database username (e.g., 'postgres' for local development).
DB_USER=postgres

# Database password. Choose a secure and unique password.
DB_PASSWORD=your_secure_password


# ───────────────────────────────────────────────────────────────
# 🔐 AUTHENTICATION SETTINGS (JWT)
# ───────────────────────────────────────────────────────────────

# A long, random secret key used to sign JWT tokens.
# Keep this private and secure. Never commit it to version control.
JWT_SECRET=your_super_secret_jwt_key

# The issuer (iss) claim to identify the token's origin.
# Set this to your domain or app identifier (e.g., 'https://api.example.com')
JWT_ISSUER=https://api.example.com

# The lifetime (expiration time) of JWT tokens in milliseconds.
# 604800000 = 7 days (7 * 24 * 60 * 60 * 1000)
JWT_TOKEN_LIFETIME=604800000
