# ───────────────────────────────────────────────────────────────
# 📦 DATABASE CONFIGURATION (PostgreSQL)
# ───────────────────────────────────────────────────────────────

# The JDBC URL to connect to your PostgreSQL database.
# Format: jdbc:postgresql://<host>:<port>/<database_name>
JDBC_DATABASE_URL=jdbc:postgresql://localhost:5432/diglit_db

# Database username (usually 'postgres' for local development).
DB_USER=postgres

# Database password. Use a secure and strong password.
DB_PASSWORD=your_secure_password

# ───────────────────────────────────────────────────────────────
# 🔐 AUTHENTICATION SETTINGS (JWT)
# ───────────────────────────────────────────────────────────────

# A strong, secret key used to sign JWT tokens.
# Use a long, random string. Keep this safe and never expose it publicly.
JWT_SECRET=your_super_secret_jwt_key

# The lifetime of JWT tokens in milliseconds.
# Example: 604800000 = 7 days (7 * 24 * 60 * 60 * 1000)
JWT_TOKEN_LIFETIME=604800000
